{
  "name": "elmer",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/horn-sim/elmer/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:simulation"],
  "implicitDependencies": [
    "types",
    "fs"
  ],
  "// targets": "to see all targets run: nx show project elmer --web",
  "targets": {
    "build": {
      "dependsOn": ["^build"]
    }
  }
}
